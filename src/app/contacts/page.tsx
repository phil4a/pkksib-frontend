import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Контакты',
	description: 'Контакты ПКК'
};

export default function ContactsPage() {
	return (
		<div>
			<h1>Контакты</h1>
		</div>
	);
}
